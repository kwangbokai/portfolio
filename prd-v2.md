# 작업물 한 장 — 설계도 v2 · 앞으로의 방향

> 이 문서는 지금 실행하는 문서가 아닙니다.
> v1이 돌아간 다음, 어디로 키워갈지 고민할 때 여는 지도입니다.
> **여기 있는 게 다 정해져 있지 않은 게 정상입니다.**

## 어디로 가고 싶은가

**작업물 상세 화면.** 지금은 카드를 누르면 노션·비핸스 같은 외부로 나가는데, 그 내용을 사이트 안으로 들여오고 싶다. 채용용 한 장에서 **작업물 아카이브** 쪽으로 무게가 옮겨가는 방향이다.

작업물 개수가 늘어난다는 전제가 깔려 있다. 지금은 3~5개지만 계속 쌓일 거라고 보고 있다.

## 붙일 후보들

> 우선순위는 아직 정하지 않습니다. 후보만 모아둡니다.

| 후보 | 왜 필요해졌나 | 크기 |
|---|---|---|
| **작업물 상세 화면** | 외부 링크로 내보내면 보는 사람이 이탈한다. 노션은 로딩도 느리고 내 사이트처럼 안 보인다 | 큼 |
| **이미지 파일 업로드** | 주소 붙여넣기를 하려면 이미지를 어딘가에 먼저 올려야 한다. 작업물이 늘수록 이 과정이 번거로워진다 | 중간 |
| **검색 · 태그 필터** | 작업물이 20개를 넘어가면 스크롤로는 못 찾는다 | 중간 |
| **커스텀 도메인** | 채용 담당자에게 `xxx.vercel.app` 을 보내는 게 계속 걸린다 | 작음 |
| **조회수 · 방문자 통계** | 링크를 보낸 뒤 실제로 열어봤는지 궁금해진다 | 작음 |
| **이력서 PDF 다운로드** | 담당자가 파일로 저장해두고 싶어 할 수 있다 | 작음 |
| **연락 폼** | 이메일 주소만 노출하면 스팸이 붙는다 | 중간 |
| **다크모드 · 애니메이션** | 그럴듯해 보이고 싶은 마음. 급하지 않다 | 중간 |

---

## ★ 앞으로 정해야 할 것

> 이 문서의 중심입니다. **답을 쓰지 말고 질문을 씁니다.**

### 상세 내용을 어디에 둘 것인가

- **질문** — 작업물 상세 글·이미지를 **Supabase에 넣을 것인가**, 아니면 지금처럼 **외부 링크로 계속 내보낼 것인가**. 넣는다면 본문을 어떤 형태로 저장할 것인가(마크다운 한 덩어리 / 문단 여러 개 / 이미지 목록 따로).
- **왜 정해야 하나** — 이게 안 정해지면 테이블 구조를 못 잡는다. 나중에 바꾸면 이미 넣어둔 작업물을 전부 손봐야 한다. 어드민(v3)도 여기에 맞춰 화면이 달라진다.
- **언제쯤 정하면 되나** — **v2를 시작할 때 가장 먼저.** 다른 결정이 여기에 딸려 온다.

### 이미지를 언제까지 주소 붙여넣기로 버틸 것인가

- **질문** — 이미지 업로드를 붙일 것인가, 붙인다면 어디에(Supabase Storage / 외부 이미지 호스팅). 아니면 계속 주소 붙여넣기로 갈 것인가.
- **왜 정해야 하나** — 상세 화면을 만들면 작업물 하나당 이미지가 여러 장 붙는다. 지금 방식(주소 하나씩 붙여넣기)은 이미지가 3장만 넘어가도 손이 많이 간다. 업로드를 붙이면 어드민 화면이 눈에 띄게 커진다.
- **언제쯤 정하면 되나** — 상세 화면 방향이 정해진 다음. 그전에 정해봐야 소용없다.

### 이 사이트를 공개로 둘 것인가

- **질문** — 검색에 걸리는 **내 이름 사이트**로 둘 것인가, 아니면 **링크를 아는 사람만** 보는 채용용 페이지로 둘 것인가.
- **왜 정해야 하나** — 공개로 가면 도메인·통계·검색 노출이 의미가 생기고, 글의 톤도 "지원자"가 아니라 "만드는 사람"으로 바뀐다. 반대로 비공개로 두면 회사마다 작업물을 바꿔 끼우는 게 자유로워진다. **둘은 같이 못 간다.**
- **언제쯤 정하면 되나** — 취업이 정리되는 시점. 지금 정하면 오히려 손해다.

### 작업물이 몇 개까지 늘어날 것인가

- **질문** — 대표작 5~8개로 계속 유지할 것인가, 아니면 만든 걸 다 쌓아두는 아카이브로 갈 것인가.
- **왜 정해야 하나** — 5~8개면 검색·태그·페이지 나누기가 전부 필요 없다. 계속 쌓는 쪽이면 그 셋이 한꺼번에 필요해지고, 어드민도 목록 관리가 무거워진다.
- **언제쯤 정하면 되나** — 작업물이 실제로 10개를 넘어갈 때. 미리 정하면 안 쓸 걸 만들게 된다.

---

## 갈림길

> 여기서 어느 쪽으로 가느냐에 따라 만드는 게 크게 달라집니다.

### 상세를 사이트 안에 둘 것인가, 외부 링크로 내보낼 것인가

| | **A · 사이트 안에 둔다** | **B · 외부 링크로 계속 내보낸다** |
|---|---|---|
| **어떤 상황** | 내 사이트에서 끝까지 보여주고 싶다. 노션 로딩·디자인이 계속 거슬린다 | 이미 노션·비핸스에 정리를 잘 해뒀다. 글 쓰는 곳을 옮기고 싶지 않다 |
| **만들 것** | 상세 페이지(`/work/[id]`) · 본문 저장 칸 · 이미지 여러 장 · 어드민에 본문 편집기 | 없음. v1 그대로 두면 된다 |
| **드는 품** | **큼** — 이 방향 하나로 v2가 다 찬다 | 작음 |
| **딸려 오는 것** | 이미지 업로드가 거의 필수가 된다. 어드민이 눈에 띄게 커진다 | 없음 |

**결정 (2026-08-17) — B · 외부 링크로 계속 내보낸다.**

어드민을 먼저 붙이고 배포까지 도달하는 쪽을 택했다. A로 가면 본문 저장 형태·이미지 업로드·어드민 본문 편집기가 한꺼번에 딸려 와서 오늘 안에 안 끝난다.

**이 결정은 되돌릴 수 있다.** `works` 표에 본문 칸을 나중에 더하면 되고, 지금 넣어둔 작업물은 그대로 쓴다. 다시 열어볼 시점은 **"노션 로딩이 거슬려서 링크를 안 누르게 될 때"** 다.

### 채용용으로 남을 것인가, 공개 포트폴리오가 될 것인가

| | **A · 채용용으로 유지** | **B · 공개 포트폴리오로** |
|---|---|---|
| **어떤 상황** | 지원할 때만 링크로 보낸다. 회사마다 작업물을 바꿔 낀다 | 내 이름으로 검색되는 사이트를 갖고 싶다 |
| **만들 것** | 없음. 지금 구조가 이미 맞다 | 커스텀 도메인 · 검색 노출 설정 · 소개글 톤 재작성 · 통계 |
| **드는 품** | 없음 | 중간 |

**지금 판단** — **아직 모름.** 취업이 정리되기 전엔 답이 안 나온다.

---

## 어드민은 어떻게

**만들었다.** (2026-08-17) `prd-v3-admin.md` 참조.

목록 관리형 + 프로필 편집. 끌어서 순서 바꾸기, 숨김 처리, Supabase 자체 로그인. `works` 표에 `is_visible` 이, 새로 `profile` 표가 늘었다.

그래서 아래 "붙일 후보들" 중 **이미지 업로드**의 성격이 조금 달라졌다 — 이제 어드민에서 이미지 주소를 붙여넣는 자리가 생겼으니, 업로드가 없어도 예전만큼 불편하진 않다. 우선순위가 한 칸 내려갔다고 보면 된다.

---

## 지금 답하지 않아도 되는 것

> 미리 고민하면 오히려 손해인 것들.

- **커스텀 도메인** — 공개로 갈지 말지가 정해진 뒤에 사도 늦지 않다. 먼저 사두면 방향이 거기에 끌려간다
- **검색 · 태그 필터** — 작업물이 20개를 넘기 전엔 만들어도 쓸 일이 없다
- **조회수 · 통계** — 붙이는 데 10분이면 된다. 언제든 붙일 수 있으니 미룰수록 이득
- **다크모드 · 애니메이션** — 구조가 다 정해진 다음에 마지막으로 얹는 게 맞다. 먼저 하면 구조 바꿀 때마다 다시 손봐야 한다
- **연락 폼** — 이메일 주소로 실제 스팸이 오기 시작하면 그때 정하면 된다

## v2에서도 하지 않을 것

- 제대로 된 회원가입 · 소셜 로그인 (어드민은 비밀번호 하나로 충분)
- 결제
- 모바일 앱
- 권한 등급 여러 개 — 바꾸는 사람은 나 하나다
