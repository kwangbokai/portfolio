# 작업물 한 장 — 설계도 v3 · 어드민

> **상태: 결정 완료 · 구현 완료** (2026-08-17)
> 비워뒀던 「아직 정하지 못한 것」에 답이 모두 채워졌고, 그대로 만들었습니다.
> 아래는 무엇을 어떻게 정했는지에 대한 기록입니다.

## 왜 어드민이 필요한가

작업물을 **수시로 추가·수정할 거고, 폰에서도 바꾸고 싶다**고 했다. v1에는 편집 화면이 없어서 Supabase 대시보드에 직접 들어가야 했는데, 그게 폰에서 특히 불편하다. 바꾸는 사람은 **나 하나**라서 어드민은 가장 작게 만들었다.

## 유형 — B · 목록 관리형 (+ A · 콘텐츠 편집형)

작업물은 **개수가 늘어나는 것**이라 목록 관리형이 뼈대다. 여기에 상단 프로필을 고치는 콘텐츠 편집형을 얹었다.

| | |
|---|---|
| **누가 쓰나** | 나 혼자 |
| **무엇을 바꾸나** | `works` 다섯 칸(`title` · `summary` · `image_url` · `link_url` · `sort_order`) + `is_visible`, 그리고 `profile` 전체 |
| **못 바꾸게 할 것** | 레이아웃 · 색 · 페이지 구조 · 칸 이름 자체 |
| **들어가는 법** | **Supabase 자체 로그인** (이메일 + 비밀번호) |

## 화면

| # | 화면 | 무엇이 보이나 | 무엇을 할 수 있나 |
|---|---|---|---|
| 1 | 로그인 (`/admin`) | 이메일 · 비밀번호 | 들어가기 |
| 2 | 작업물 탭 | `sort_order` 순 목록 — 손잡이 · 썸네일 · 제목 · 설명 | 추가 · 수정 · 숨기기/보이기 · **끌어서 순서 바꾸기** |
| 3 | 작업물 폼 | 다섯 칸 + "사이트에 보이기" 체크 | 채우고 저장 |
| 4 | 프로필 탭 | 이름 · 직군 · 소개 · 이메일 · 외부 링크 목록 | 고치고 저장 |

---

## ★ 정한 것 — 원래 비워뒀던 질문들

### 1. 순서를 어떻게 바꾸나 → **끌어서 옮기기**

손잡이(⠿)를 **따로 뒀다.** 행 아무 데나 잡아도 끌리게 하면 폰에서 스크롤과 부딪히기 때문이다. 손잡이를 잡으면 끌기, 다른 데를 잡으면 평소대로 스크롤.

포인터 이벤트로 만들어서 **마우스와 터치가 같은 코드로 동작**한다. 놓는 순간 화면 순서대로 `sort_order` 를 1,2,3… 으로 다시 매기고, **실제로 값이 바뀐 줄만** 저장한다.

### 2. 지운 항목은 어떻게 하나 → **숨김 처리**

`is_visible` 칸을 하나 더 뒀다. 끄면 사이트에서만 사라지고 데이터는 남는다. 폰에서 오터치가 나도 되돌릴 수 있다.

> **완전 삭제 버튼은 일부러 안 만들었다.** 정말 지워야 할 게 생기면 Supabase 대시보드에서 지우면 된다. 목록이 숨긴 항목으로 지저분해지면 그때 붙이자.

### 3. 상단 프로필까지 바꾸나 → **바꾼다**

`profile` 표(한 줄짜리)를 만들었다. 이름 · 직군 · 소개 · 이메일 · 외부 링크 목록까지 어드민에서 고친다. 소개 문구를 바꾸겠다고 배포를 다시 할 일이 없어졌다.

사이트는 `profile` 표를 먼저 읽고, **못 읽으면 `config.js` 값으로 떨어진다.** 어느 쪽이 망가져도 화면의 이름 칸이 비지 않는다.

### 4. 이미지는 어떻게 하나 → **주소 붙여넣기 유지**

업로드는 안 붙였다. `prd-v2.md` 의 갈림길에서 **상세 화면을 외부 링크로 유지**하기로 했기 때문에, 작업물 하나당 이미지가 한 장이면 충분하다. 업로드는 그대로 v2 후보로 남는다.

### 5. 비밀번호를 어디에 두나 → **Supabase 자체 로그인**

원래 보기에 있던 "직접 만든 비밀번호 하나"를 **쓰지 않았다.** 이유가 둘이다.

- 직접 만든 비밀번호로 쓰기를 열려면 **서버가 필요하다.** 강력한 키(service_role)를 서버에 두고 거기서만 쓰기를 해야 하는데, Node 가 없어서 그 서버 코드를 **로컬에서 검증할 방법이 없었다.** 확인 못 한 코드를 배포로 밀어넣게 된다
- Supabase 자체 로그인을 쓰면 **서버 코드가 아예 없다.** 누가 무엇을 할 수 있는지는 데이터베이스의 보안 설정(RLS)이 정한다. 브라우저 코드를 아무리 뜯어봐도 로그인 없이는 아무것도 못 쓴다

여전히 **회원가입은 만들지 않았다.** 계정은 Supabase 대시보드에서 내 것 하나만 손으로 만들고, 신규 가입은 꺼둔다. 그래서 "로그인은 안 만든다"는 원래 원칙과 어긋나지 않는다.

---

## 보안 — 누가 무엇을 할 수 있나

`supabase-setup.sql` 이 정하는 내용이다. **이게 이 어드민 보안의 전부다.**

| | 방문자 (로그인 안 함) | 나 (로그인함) |
|---|---|---|
| `works` 읽기 | **켜둔 것만** | 숨긴 것까지 전부 |
| `works` 추가·수정·삭제 | ❌ | ⭕ |
| `profile` 읽기 | ⭕ | ⭕ |
| `profile` 수정 | ❌ | ⭕ |

`config.js` 에 든 키(anon key)는 **공개돼도 되는 값**이다. 위 표의 "방문자" 권한만 갖는다.

## 완성 기준

- [ ] 로그인을 통과해서 어드민에 들어간다
- [ ] 폰에서 작업물을 하나 추가하면 사이트에 바로 뜬다
- [ ] 작업물을 수정할 수 있다
- [ ] 숨기면 사이트에서 사라지고, 다시 켜면 돌아온다
- [ ] 손잡이를 끌어 순서를 바꾸면 사이트 순서도 바뀐다
- [ ] 프로필을 고치면 사이트 상단이 바뀐다
- [ ] 로그아웃 상태에서 `/admin` 에 들어가면 로그인 화면에서 막힌다
- [ ] **v1에서 되던 게 그대로 된다**

## 앞으로 붙일 만한 것

- 완전 삭제 버튼 (숨긴 항목이 쌓이면)
- 이미지 업로드 (`prd-v2.md` 참조)
- 작업물이 20개를 넘으면 어드민 목록에도 검색
