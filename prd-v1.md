# 작업물 한 장 — 설계도 v1

> 오늘 만들어서 배포까지 갑니다. 여기 없는 건 오늘 만들지 않습니다.

## 한 줄 요약

지원한 회사의 채용 담당자가 링크 하나로 내 대표 작업물을 훑어볼 수 있는 한 장짜리 페이지.

## 왜 만드나

이력서만으로는 실제로 뭘 만들었는지가 안 보인다. 링크 하나만 보내면 대표작이 바로 펼쳐지게 만들려고 한다. 작업물은 계속 늘어나고 회사마다 바꿔 끼우고 싶어서, 내용을 코드가 아니라 데이터베이스에 둔다.

## 누가 보나

**지원한 회사의 채용 담당자.** 이력서를 이미 받은 상태에서 "이 사람 좀 더 볼까" 하고 링크를 여는 사람. 오래 머물지 않는다.

## 여기서 딱 한 가지를 한다면

**작업물을 위에서 아래로 훑어본다.** 마음에 드는 게 있으면 카드를 눌러 외부 상세 페이지로 넘어간다.

## 화면

| # | 화면 | 여기서 보이는 것 | 여기서 할 수 있는 것 |
|---|---|---|---|
| 1 | 한 장 페이지 (`/`) | **상단** — 이름 · 직군 한 줄 · 한두 문장 소개 · 이메일 주소 · 외부 링크 1~3개(링크드인·깃허브 등)<br>**본문** — 작업물 카드 목록. `순서` 오름차순으로 세로 나열. 카드마다 이미지 · 제목 · 한 줄 설명 | 스크롤해서 훑어본다<br>카드를 눌러 `링크`로 이동(새 탭)<br>이메일 주소를 눌러 메일 앱 열기 |

화면은 이 한 장이 전부다. 다른 경로를 만들지 않는다.

## 내용을 저장하나

- [ ] 저장 안 함 — 보여주기만
- [x] **저장함** — 아래 표 사용

### 저장할 내용

테이블 이름: `works` (작업물)

| 칸 이름 | 무엇 | 예시 |
|---|---|---|
| `title` | 작업물 제목 | `배민 리브랜딩 캠페인` |
| `summary` | 한 줄 설명. 역할·연도까지 여기 녹인다 | `2025 · 메인 카피 담당` |
| `image_url` | 썸네일 이미지 **주소**. 업로드가 아니라 URL 붙여넣기 | `https://.../thumb.jpg` |
| `link_url` | 상세를 볼 외부 주소 (노션 · 비핸스 · 깃허브 등) | `https://notion.so/...` |
| `sort_order` | 숫자가 작을수록 위로 | `1` |

**칸을 더 늘리지 않는다.** 태그 · 카테고리 · 조회수 전부 v2다.

**반드시 짚을 것 — Supabase RLS.** Supabase는 보안 설정(RLS)이 기본으로 켜져 있어서, 이걸 안 잡으면 **데이터를 분명히 넣었는데 화면에 아무것도 안 뜨는** 일이 생긴다. `works` 테이블에 **누구나 읽기(select)만 가능한** 정책을 만들어 둘 것. 쓰기(insert · update · delete) 정책은 만들지 않는다 — 오늘 페이지는 읽기만 한다.

**오늘 내용을 넣는 방법.** 편집 화면은 오늘 만들지 않는다. 작업물 3~5개를 Supabase 대시보드의 테이블 편집기에서 직접 입력한다. 폰에서 편하게 고치는 건 v3 어드민이 붙어야 완성된다.

## v1에 넣지 않는 것

> 자르는 게 아니라 v2로 넘겼습니다. `prd-v2.md` 에 왜 필요했는지까지 적혀 있습니다.

- 이미지 **파일 업로드** (오늘은 주소 붙여넣기)
- 어드민 편집 화면 (→ `prd-v3-admin.md`)
- 연락 폼 · 메시지 받기 (오늘은 이메일 주소 노출까지)
- 이력서 PDF 다운로드
- 조회수 · 방문자 통계
- 다크모드 · 스크롤 애니메이션
- 커스텀 도메인 (배포 주소로 충분)
- 검색 · 태그 필터
- 작업물 상세 화면 (외부 링크로 대체)

## 완성 기준

- [ ] Vercel 배포 주소가 열린다
- [ ] Supabase `works` 에 넣은 작업물이 페이지에 **순서대로** 보인다
- [ ] 카드를 누르면 외부 링크가 새 탭으로 열린다
- [ ] 폰 화면에서 레이아웃이 깨지지 않는다
- [ ] Supabase에서 작업물을 하나 추가하고 새로고침하면 화면에 늘어나 있다

## 만들 때 쓰는 것

- 배포: **Vercel**
- 저장: **Supabase** (`works` 테이블 하나)

## 만들 때 지킬 것

- 화면에 보이는 글자는 전부 한국어로
- 모바일에서도 깨지지 않게 — 채용 담당자는 폰으로 열 확률이 높다
- 완성보다 배포가 먼저. 배포 주소가 열리면 오늘 목표는 달성이다
- 이미지 주소가 비어 있거나 깨져도 카드가 무너지지 않게 처리할 것
- 작업물이 아직 하나도 없을 때 빈 화면이 아니라 안내 문구가 보이게 할 것
- **나중에 어드민을 붙일 예정이니, 자주 바뀔 글자(상단의 이름 · 소개 · 이메일 · 외부 링크)는 코드 여기저기 흩지 말고 한곳에 모아둘 것**
